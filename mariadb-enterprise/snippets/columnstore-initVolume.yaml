- name: init-volume
  image: {{ .Values.mariadb.columnstore.image }}
  command:
    - bash
    - "-c"
    - "[ ! -d \"/mnt/columnstore/etc\" ] && rm -rf /mnt/columnstore/data && cp -rp /usr/local/mariadb/columnstore/data /mnt/columnstore/ && rm -rf /mnt/columnstore/local && cp -rp /usr/local/mariadb/columnstore/local /mnt/columnstore/ && cp -rp /usr/local/mariadb/columnstore/etc /mnt/columnstore"
  volumeMounts:
    - name: data
      mountPath: /mnt/columnstore
